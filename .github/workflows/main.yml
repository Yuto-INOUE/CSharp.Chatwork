name: Publish nuget Package

on:
  push:
    branches: master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Setup .NET Core SDK
        uses: actions/setup-dotnet@v1
        with:
          source-url: https://api.nuget.org/v3/index.json
          dotnet-version: 3.1.101

      - name: Build
        run: dotnet build ./CSharp.Chatwork/CSharp.Chatwork.csproj -c Release
        
      - name: Push for nuget
        run: dotnet nuget push ./CSharp.Chatwork/bin/Release/CSharp.Chatwork.*.nupkg --api-key $NUGAET_API_KEY
        env:
          NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }} 
